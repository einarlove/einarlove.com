%title= @description
%meta{charset: "utf-8"}
= stylesheet_link_tag "reset", :media => "all"
= stylesheet_link_tag "main", :media => "all"
= csrf_meta_tags
= render "shared/typekit"
- unless request.path == "/login"
	%header
		%hgroup
			%a{ href: "/", title:"Back to frontpage"}
				%h1 Einar Löve
				%h2 Interactiondesigner
		%nav
			= render "shared/navigation"
%section.admin_area
	= yield